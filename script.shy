image {
    xp = 0;
    yp = (y - 0.5) * (y - 0.5);

    if x > 0 {
        xp = (x - 0.5) * (x - 0.8);
    } else {
        xp = (x - 0.2) * (x - 0.5);
        return (1 - xp / yp, 2.0 + xp / yp, xp / yp);
    };
}
